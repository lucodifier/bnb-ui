import * as React from "react";
import { Button, Card, Grid, Typography } from "@material-ui/core";

export  function NewPayment() {

    return (
        <React.Fragment>
          <Typography>Enviar pagamento para:</Typography>
        </React.Fragment>
      );
    }